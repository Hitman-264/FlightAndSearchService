/
    -src/
        index.js //server
        models/
        controllers/
        middlewares/
        services/
        utils/
        config/
    -tests/[later]
    -static/
    -temp/

    //actual server logic reside in src folder.
    we don't upload tests, static, temp..
    it will be lighter booting.